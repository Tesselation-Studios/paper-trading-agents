# Pending Action Items

The carry-forward mechanism the 2026-07-26 weekly review called for: *"items flagged in nightly syntheses (NVDA trim, sentiment escalation, homework stack) don't get executed because the next session's tick agent never reads the prior synthesis... A carry-forward mechanism (tasks/pending.md or active.md section) would close it."*

This file exists so a proposal made in one reflection session (nightly-maintenance, nightly-learning, or an off-hours note) survives into the next one instead of rotting in a journal entry nobody re-reads. Keep it **short** — open items only, plain checklist, resolved items get removed (the reasoning/outcome belongs in the journal entry or commit that resolved it, not here).

**Read this at the start of every nightly-maintenance / nightly-learning / weekly-review run.** For each open item: either resolve it (make the change, remove the line), or re-affirm it's still worth doing (leave it, don't just silently re-copy it — if it's been open more than ~2 weeks with no action, that's itself worth a line in the journal: is it actually not worth doing?).

**Write a new item here** whenever a reflection session identifies something concrete worth doing that isn't being done in the same session — a code change to propose, a param to reconsider, an escalation that needs a human, a pattern worth testing next off-hours window. One line, dated, sourced. Not for routine "watch this ticker" notes — those belong in active.md/positions/*.md, this file is for process/strategy follow-ups.

Format: `- [ ] YYYY-MM-DD (source): description`

---

## 🔴 MONDAY PRE-FLIGHT (Aug 10, 2026) — ALL RESOLVED

- [x] Position DB reconciliation: CLIR correctly closed in DB (hard stop Aug 4) — not in active.md. MBBC phantom Alpaca position created in DB (1sh @ $15.30). BL qty fixed (1→2, entry $30.00). reconcile_positions.py now clean (0 critical, 0 warnings). Root cause: script never wired into off-hours cron — fixed by Raf.
- [x] CLIR open price check: closed Aug 4 at $3.96 (hard stop breach). Not in active.md. Resolved.
- [x] Sell VSXY at Monday open: executed earlier today (+10.80% realized, $27.45 gains).
- [x] Discovery daemon health: N/A — watchlist unblinded by merge_discoveries.py fix (Claude Code session).
- [x] Index-anchor deployment: EXECUTED 1:02 PM ET Aug 10. SPY 1sh @ $773.09 conviction play, reconcile 0.6111 bullish/agree/signal_count:4. Stop $726.70. Re-entered 1:18 PM after check_stops() conviction-cap bug fix (c2aff0b). IWM scored 0.5833, held. Springboard guidance from Raf: actively rotate out into individual positions.

---


## Open

- [ ] 2026-08-11 (tick-replay): prepare_tick_replay.py _snapshot_at filter requires both rsi_14 AND macd_hist non-NaN — on older cached bar dates (e.g. 2026-06-29, practice-chain-1), this silently drops 89% of the universe (6/57 symbols survived). HRB and AMD open positions had zero intraday or EOD coverage. A fallback showing close+volume when technicals are unavailable would make these days usable. Either add a permissive fallback to _snapshot_at or compute indicators more aggressively during bar sync. → Workboard card `67d071d3` (2026-08-12).


- [ ] 2026-08-11 (claude-code-session): ACXP closed 2026-08-11 15:42 ET with realized_pnl == $0.00 exactly (close_reason "trailing stop breached: -8.6% off peak .63, stop .52" — clearly a real loss, not actually a $0 exit). Caught live by the new check_zero_pnl_closes() detection (scripts/workspace_review.py) the same day it shipped — a THIRD occurrence of this bug shape after ZBRA/DXCM/OOMA (Aug 3) and VSXY/FLXS/CLIR (Aug 10), via a not-yet-identified cause (bankroll_history confirms the same $0.00 was recorded there too, so this isn't a positions-table-only mislabel — close_trade_outcome() itself computed pnl=0 for this trade). Needs root-cause investigation (was this the reconcile_stopped_out_positions() broker-fill path? a bad Alpaca fill lookup?) then a backfill correcting positions.realized_pnl + bankroll_state + experience.json together, same rigor as the two prior backfill scripts. Not fixed in this session — flagged, not guessed at. → Workboard card `6b70db87` (2026-08-12, high priority).
- [ ] 2026-08-11 (claude-code-session): experience.json total_wins/total_losses (22W/35L=57) has diverged from bankroll_state's lifetime_wins/lifetime_losses (25W/35L=60) by 3 whole closed trades, not just a reclassification (losses match exactly at 35, so this isn't the "backfill script skipped experience.json" story it first looked like — that would leave the total unchanged, not create a 3-trade gap). Real cause still open; plausible suspect is an unguarded concurrent write race in record_experience_outcome()/_save_experience() (plain file read-modify-write, no fcntl lock, unlike executor.py's own _order_lock() for the same class of problem) — not confirmed. workspace_review.py's new check_experience_bankroll_sync() flags the divergence going forward; TOOLS.md/bankroll.py docstrings updated to note the intended invariant. Needs someone to trace which 3 trades experience.json is missing before attempting a backfill. → Workboard card `ecd4cc25` (2026-08-12).
- [x] 2026-08-10 (nightly-learning): SPY index-anchor sizing constraint — at ~$10.4K portfolio, 1 SPY share = 7.4% (>6% cap). → RESOLVED 2026-08-12 (claude-code-session): documented directly in `decision_heuristics.md`'s `index_anchor_entry_v1` Sizing guidance (the upside exemption only covers a position already held growing past cap organically — the initial BUY still has to clear `gate_position_size` at order time). Not a bug, was just undocumented.

- [ ] 2026-08-03 (nightly-maintenance): Track watchlist-to-entry conversion rate by regime. CHOPPY converted at ~2% Aug 3, 0% Aug 4, 6 entries from 7 priced on Aug 5. Need SUSTAINABLE comparison data. ⚠️ No data Aug 6-7 (outage).
- [ ] 2026-08-03 (tick-replay): CHOPPY index ≠ every name chops — evaluate softening "CHOPPY suppresses all entries" to "CHOPPY suppresses broad sweep but individual names with independent confirmed setups can still enter." Needs more data.
- [ ] 2026-08-02 (weekly-review): Peak entry timing rule — track RDDT as occurrence #1. If a second comparable loss from early-session momentum-spike entries, harden into strategy.md.
- [ ] 2026-08-02 (weekly-review): Exit discipline rule — track +3%+ intraday gains fading to negative/stop. AMD, RDDT. BJDX (+6.1% phantom) NOT counted. Still need 1 more real occurrence before hardening.
- [ ] 2026-08-02 (weekly-review): Small-cap vs large-cap universe evidence — now VERY HIGH confidence per the Aug 9/10 overnight cycles (9+ rounds combined): core/small-cap universes return ~2.4x broad/large-cap (8.78% vs 3.60% avg return across runs). Still a weekend discussion item for Raf (universe composition, not something to change unilaterally), but the evidence bar this item was waiting on has clearly cleared. ⚠️ 10 days stale as of 2026-08-12 purely waiting on that conversation, not more evidence — flagged directly, not a workboard item.
- [ ] 2026-08-03 (claude-code-session): Test-isolation gap in tests/test_executor_audit.py. Low-priority one-line fix.
- [ ] 2026-08-03 (claude-code-session): `trader_write.py position-update-thesis --verdict` timestamp override. One-line fix, replay-only impact.
  → Both bundled into workboard card `6aca556b` (2026-08-12, low priority) — under-specified enough (no file:line) that a confident blind fix wasn't safe.
- [ ] 2026-08-04 (nightly-learning): CLIR same-session re-entry tracking — occurrence #1 (Aug 4 re-entry) concluded Aug 10: CLIR stopped out at -7.22%, the re-entry eventually lost. Track a second occurrence of the pattern before hardening a rule either way.
- [ ] 2026-08-05 (raf-direction): Avoid new micro-cap entries until Alpaca websocket connectivity. Needs concrete threshold (price? market cap?) per Raf. ⚠️ Still open, genuinely blocked on that threshold — flagged directly, not a workboard item.
- [ ] 2026-08-05 (nightly-maintenance): BJDX micro-cap data quality — phantom position-stream spikes on sub-$3 names. Track recurrence.
- [x] 2026-08-09 (weekly-review): Momentum re-screen — Aug 7 tick-replay surfaced process gap: names flagged as "interesting but not entering" aren't re-screened at subsequent intervals. VOYG +7.44% missed. → INVESTIGATED 2026-08-12 (claude-code-session): the premise doesn't fully hold — `trader_db.get_watchlist_batch()`'s least-recently-evaluated round-robin already re-screens every watchlist name roughly every 5 ticks (~25 min at market-hours cadence, `params.json: watchlist.eval_batch_size=6` vs `max_size=30`, see that param's `_eval_batch_size_source` note). A skipped name isn't permanently skipped, it's re-queued. If VOYG's move happened and reversed faster than ~25 min, that's a real cadence-vs-speed tradeoff worth knowing about, but it's not "never re-screened" — didn't build a new mechanism for something that already exists; flagging the corrected framing instead.
- [ ] 2026-08-10 (raf-session): tick_prompt.md missing index-anchor deploy trigger — Aug 10 EVIDENCE: IWM and SPY both deployed today via decision_heuristics.md's index_anchor_entry_v1 fast-path. The mechanism IS firing from the tree node, even without a dedicated step-8 trigger. The question is now: does the tree node alone provide sufficient coverage, or was today's deployment unique because a Claude Code session (Raf) explicitly primed the system? Monitor next 2-3 CHOPPY sessions — if index-anchor deploys autonomously from the tree node without human priming, close this item.
- [x] 2026-08-11 (nightly-maintenance): Close the loop on overnight research's fixed-threshold recommendations — confirmed per Aug 9 insights doc: optimizer's recommendations are constructs of its simplified simulated strategy, not applicable to live gestalt logic. Resolved in Aug 11 journal entry. Future overnight cycles: disregard binary-threshold recommendations unless they reference live pipeline fields (which they currently don't).
- [ ] 2026-08-11 (research-triage): 7-period RSI as an alternative signal, worth testing (not adopting) — recommended across both the Aug 9 and Aug 10 overnight cycles (run 5: 4.45% catch rate, 0% false positives on loose-conviction/short-RSI stonks). Live technical data only exposes `rsi_14` (`bar_loader.py`/`discovery_screen.py`) — there's no `rsi_7` field anywhere in the live pipeline, so this needs a data-bus-side change (paper-trading-rebuild computing and exposing a second RSI period) before it's even testable, not a params.json tweak. Track as an evidence-gated experiment per this system's usual bar (2-3 occurrences) before hardening, same as any other strategy.md candidate — don't wire it in off one overnight run. → Workboard card `bfb46bc5` (2026-08-12, low priority, cross-repo).
- [x] 2026-08-11 (nightly-learning): HPK deployment-pressure gate-override — occurrence #1. Agent entered HPK 17sh @ $8.39 against a clear `[tree:catalyst_led_entry_v1]` rejection. At close: $7.92 (-5.56%), $0.06 above $7.86 hard stop. The tree was correct and the entry was wrong. → SUPERSEDED by new item below (merged into the broader deployment-pressure tracking)
- [x] 2026-08-11 (nightly-maintenance): Deployment-pressure overrides — 4 occurrences (OLP Jul 30, MBBC Aug 3, MCFT Aug 10, HPK Aug 11). All negative. → RESOLVED 2026-08-12 (claude-code-session): `deployment_pressure_override_v0` promoted watch→active with a hard HOLD leaf, decision_heuristics.md. This had been escalated via Telegram 3+ times with no one actually committing it — the real fix was closing the gap between "escalate" and "commit," not re-escalating again. See `skills/rule-mechanization-audit.md`'s new tree-fix/code-fix split and `evolution_proposal.py`'s AUTO_TIER_FILES change (2026-08-12) that made this self-committable in the first place.
- [ ] 2026-08-11 (nightly-maintenance): HPK tree-rejection override — occurrence #1 of entering against explicit `[tree:catalyst_led_entry_v1]` rejection. Track for occurrence #2 before hardening "tree rejections are hard stops — no gestalt override" into strategy.md/decision_heuristics.md. Distinct from deployment-pressure pattern (this is specifically about overriding a gate's NO, not entering on weak signal).
- [x] 2026-08-11 (nightly-maintenance) + 2026-08-11 (research-triage, duplicate of the same finding): Technology pipeline gap — 75% win rate (6W/2L per signal_scorecard), zero discovery pipeline candidates. → INVESTIGATED 2026-08-12 (claude-code-session), not fixed: no code-level Technology exclusion exists anywhere in the discovery pipeline — `universe_scan.py` random-samples all NYSE/NASDAQ common stock with no sector filter, `discovery_screen.py`'s price ($1-$500)/RSI (45-65) bands are sector-agnostic, and sector is tracked nowhere in `discovery_db.py`'s pool schema (only known at BUY time via `--sector`). The exclusion hypothesis is refuted. Real gap is observability, not screening: "zero candidates" is Stan's own impression from batch reads, not a measurable fact, since sector isn't tagged until purchase — could be true or could be sampling variance in the random-universe rotation. A real fix needs live `get_fundamentals` sector-tagging at promotion time (can't build/test standalone outside a live agent session) — flagging as a real follow-up, not closing the door on it, just not guessing at a fix with no code-level evidence behind it.
- [ ] 2026-08-11 (nightly-maintenance): BWMN data coverage gap — 58% PE acquisition missed by Alpaca news feed. Framework correctly identified the stock, correctly gated on absent sentiment, but the catalyst was real and invisible. Wider data sourcing needed. Escalate to Raf. ⚠️ Still open, genuinely needs Raf's input (a data-source/vendor decision) — flagged directly, not a workboard item.
- [ ] 2026-08-12 (claude-code-session): Research hypothesis — regime-gated position-adding for individual names. `get_market_regime` (daily K-Means, live since 2026-08-10) currently only gates the 2 index-anchor trades (SPY/IWM) via `index_anchor_entry_v1`/`index_anchor_thesis_break_v1` — decorative for the other 66+ trades. The reverted scale-into-winners experiment (v1.20, Aug 5) failed specifically because it added to positions unconditionally every tick regardless of regime — H1 (choppier) Sharpe 0.219 vs H2 (more trending) Sharpe 2.362, a ~10x gap. That's a strong prior that a regime-gated version (only pyramid in a confirmed momentum_bull/trending regime read, never in mean_reversion/volatility_spike/choppy) could recover the upside without the downside a blanket version had. Not proposing to re-enable scale-in — proposing this as a `stonks-research-loop` backtest hypothesis: does gating the OLD scale-in logic on `get_market_regime` clear the same split-window Sharpe-positive-both-halves bar the ungated version failed? Use `replay_check.py`'s existing scale-in machinery (still on disk, unwired) rather than rebuilding it.

---

## Resolved This Week (Aug 9 weekly review + Aug 10 live session)

- [x] 2026-08-11 (claude-code-session): Discovery signal-quality pre-filter → NOT BUILDING, premise didn't hold up. The "10,011 signals, ZERO caught" claim (run 7, Aug 9/10 overnight cycle) traced back to `src/overnight_harness.py`'s summary log rounding `catch_rate` with `%.2f` — the actual stored value was `catch_rate=0.001, false_positive_rate=0.0` (every trade taken matched a discovered signal, the opposite of "pure noise"). Fixed the log format (`%.3f%%`, commit `205f064` in paper-trading-rebuild) so future overnight cycles don't misread small-but-nonzero rates as literal zero. No multi-timeframe/directional-agreement pre-filter code exists anywhere (confirmed via grep) — was prose-only. Live discovery's real filters (RSI 45-65, vol≥0.8x) are already tighter than the "ultra-wide" config that produced the misleading number anyway.
- [x] 2026-08-10 (claude-code-session): MBBC liquidity gate item was stale — `gate_catalyst_liquidity` has actually been fully mechanized (GATES dict + tests) since 2026-08-05, this Aug 3 item just never got removed after. Confirmed no real gap. Note: this gate only blocks *new* illiquid catalyst entries — MBBC itself (already held, 1sh since Aug 5) stays stuck regardless; exiting an already-illiquid position is a different, currently-unsolved problem, not something this gate addresses.
- [x] 2026-08-10 (claude-code-session): CI's `TestScoreSentimentBatchViaWorker` (4 tests) → FIXED. Every CI run since the pipefail fix was red for this. Turned out the tests (and the real news_collector.py code path) only ever import `generated.gpu_compute_pb2` — pure protoc-generated message definitions (90 lines, depends only on `google.protobuf`), never the actual live gRPC client (`orchestrator/gpu_client.py`, which the tests mock out entirely). Vendored just the message file (`generated/gpu_compute_pb2.py`) + added `protobuf` to requirements.txt — the live-client staleness/drift risk this item originally flagged doesn't apply, since that code was never what these tests needed.
- [x] 2026-08-10 (claude-code-session): Pipeline null-price rot → ROOT CAUSE FOUND AND FIXED. `scripts/merge_discoveries.py`'s TICKER_HEADER_RE had no capture group for price. 68-81% watchlist silently rejected since Aug 3. Fixed regex + backfill + regression test.
- [x] 2026-08-10 (raf-session): check_stops() oversized-position trim bug → FIXED (c2aff0b). Conviction plays now correctly evaluated against their own 10% cap instead of flat 6%. SPY re-entered after fix.
- [x] 2026-08-10 (raf-session): reconcile_positions.py cron gap → FIXED. Script was never wired into off-hours prompt, silently never ran. Added as Step 1. MBBC/BL drift resolved manually.
- [x] 2026-08-07 (nightly-learning): MACDh fallback investigation → DROPPED. 5+ weeks reliable, total outage confirmed uselessness.
- [x] 2026-08-07 (nightly-maintenance): Scale-into-winners strategy.md note → RESOLVED. v1.20 already removed it.
- [x] 2026-08-02 (weekly-review): v1.7-gentle scale-in cap proposal → WITHDRAWN. v1.20 reverted scale-in entirely. Moot.
- [x] 2026-08-04 (claude-code-session): SCALE_IN_MAX_MULTIPLE sweep → WITHDRAWN. v1.20 reverted scale-in entirely. Moot.
- [x] 2026-08-07 (nightly-learning): API outage escalation → CONDITION CLEARED. Connectivity restored, no recurrence.