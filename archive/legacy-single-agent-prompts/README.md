# Legacy single-agent prompts (archived)

Pre-per-trader-directory era: a single shared AGENTS.md/HEARTBEAT.md/TOOLS.md
template (data-bus-at-localhost:5000, 1-share fast trades, generic momentum
instructions) that predates the `kairos/`/`aldridge/`/`stonks/` split. Nothing
reads these anymore — kept for history, not as a reference.
